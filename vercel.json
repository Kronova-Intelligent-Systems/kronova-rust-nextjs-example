{
  "functions": {
    "app/api/**/*.rs": {
      "runtime": "vercel-rust@4.0.8"
    }
  },
  "buildCommand": "rm -rf .next node_modules/.cache && bun install --force && bun run build",
  "installCommand": "bun install --force"
}
